LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} ClimateParams.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(ICLIM SHARED ${SRC_LIST})
IF(WIN32)
	TARGET_LINK_LIBRARIES(ICLIM util module_setting)
	SET_TARGET_PROPERTIES(ICLIM PROPERTIES COMPILE_FLAGS "/openmp")
	SET_PROPERTY(TARGET ICLIM PROPERTY FOLDER "modules/climate")
ELSE()
	TARGET_LINK_LIBRARIES(ICLIM libutil libmodule_setting)
ENDIF(WIN32)