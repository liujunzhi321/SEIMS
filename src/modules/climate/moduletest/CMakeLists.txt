LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} moduletest.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(moduletest SHARED ${SRC_LIST})
IF(WIN32)
	TARGET_LINK_LIBRARIES(moduletest util module_setting)
	SET_TARGET_PROPERTIES(moduletest PROPERTIES COMPILE_FLAGS "/openmp")
	SET_PROPERTY(TARGET moduletest PROPERTY FOLDER "modules/climate")
ELSE()
	TARGET_LINK_LIBRARIES(moduletest libutil libmodule_setting)
ENDIF(WIN32)
