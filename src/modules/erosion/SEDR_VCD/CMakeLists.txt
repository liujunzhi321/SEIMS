LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} SEDR_VCD.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(SEDR_VCD SHARED ${SRC_LIST})
IF (MSVC)
    SET_PROPERTY(TARGET SEDR_VCD PROPERTY FOLDER "modules/erosion")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(SEDR_VCD module_setting bmps data util)
ELSE ()
    TARGET_LINK_LIBRARIES(SEDR_VCD libmodule_setting.a bmps.a libdata.a libutil.a)
ENDIF ()