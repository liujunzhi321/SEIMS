LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} KinWavSed_CH.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(KINWAVSED_CH SHARED ${SRC_LIST})
IF (WIN32)
    TARGET_LINK_LIBRARIES(KINWAVSED_CH util module_setting)
    SET_TARGET_PROPERTIES(KINWAVSED_CH PROPERTIES COMPILE_FLAGS "/openmp")
    SET_PROPERTY(TARGET KINWAVSED_CH PROPERTY FOLDER "modules/erosion")
ELSE ()
    TARGET_LINK_LIBRARIES(KINWAVSED_CH libutil libmodule_setting)
ENDIF (WIN32)