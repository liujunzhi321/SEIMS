LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} MUSLE_AS.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(MUSLE_AS SHARED ${SRC_LIST})
IF (WIN32)
    TARGET_LINK_LIBRARIES(MUSLE_AS util module_setting)
    SET_TARGET_PROPERTIES(MUSLE_AS PROPERTIES COMPILE_FLAGS "/openmp")
    SET_PROPERTY(TARGET MUSLE_AS PROPERTY FOLDER "modules/erosion")
ELSE ()
    TARGET_LINK_LIBRARIES(MUSLE_AS libutil libmodule_setting)
ENDIF (WIN32)