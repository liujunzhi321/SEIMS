LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} VSM_CH.cpp reach.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(VSM_CH SHARED ${SRC_LIST})
IF (MSVC)
    SET_PROPERTY(TARGET VSM_CH PROPERTY FOLDER "modules/hydrology_longterm")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(VSM_CH module_setting bmps data util)
ELSE ()
    TARGET_LINK_LIBRARIES(VSM_CH libmodule_setting.a bmps.a libdata.a libutil.a)
ENDIF ()