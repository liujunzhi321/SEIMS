LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} managementOperation_SWAT.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(MGT_SWAT SHARED ${SRC_LIST})
IF (WIN32)
    TARGET_LINK_LIBRARIES(MGT_SWAT module_setting.lib bmps.lib data.lib util.lib bson-1.0.lib mongoc-1.0.lib)
    IF (MSVC)
        SET_PROPERTY(TARGET MGT_SWAT PROPERTY FOLDER "modules/ecology")
    ENDIF ()
ELSE ()
    TARGET_LINK_LIBRARIES(MGT_SWAT libmodule_setting.so libbmps.so libdata.so libutil.so libbson-1.0.so libmongoc-1.0.so)
ENDIF ()