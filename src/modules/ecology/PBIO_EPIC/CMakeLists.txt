LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} PotentialBiomass_EPIC.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(PBIO_EPIC SHARED ${SRC_LIST})
IF(WIN32)
	TARGET_LINK_LIBRARIES(PBIO_EPIC util module_setting)
	SET_TARGET_PROPERTIES(PBIO_EPIC PROPERTIES COMPILE_FLAGS "/openmp")
	SET_PROPERTY(TARGET PBIO_EPIC PROPERTY FOLDER "modules/ecology")
ELSE()
	TARGET_LINK_LIBRARIES(PBIO_EPIC libutil libmodule_setting)
ENDIF(WIN32)