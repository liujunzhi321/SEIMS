LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} Biomass_EPIC.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(BIO_EPIC SHARED ${SRC_LIST})
IF (WIN32)
    TARGET_LINK_LIBRARIES(BIO_EPIC util module_setting)
    SET_TARGET_PROPERTIES(BIO_EPIC PROPERTIES COMPILE_FLAGS "/openmp")
    SET_PROPERTY(TARGET BIO_EPIC PROPERTY FOLDER "modules/ecology")
ELSE ()
    TARGET_LINK_LIBRARIES(BIO_EPIC libutil libmodule_setting)
ENDIF (WIN32)