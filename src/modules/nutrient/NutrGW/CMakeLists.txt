LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} NutrientinGroundwater.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(NutrGW SHARED ${SRC_LIST})
IF(WIN32)
	TARGET_LINK_LIBRARIES(NutrGW util module_setting)
	SET_TARGET_PROPERTIES(NutrGW PROPERTIES COMPILE_FLAGS "/openmp")
	SET_PROPERTY(TARGET NutrGW PROPERTY FOLDER "modules/nutrient")
ELSE()
	TARGET_LINK_LIBRARIES(NutrGW libutil libmodule_setting)
ENDIF(WIN32)