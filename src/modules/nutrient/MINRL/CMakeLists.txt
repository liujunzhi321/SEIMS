LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} NandPmi.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(MINRL SHARED ${SRC_LIST})
IF (MSVC)
    SET_PROPERTY(TARGET MINRL PROPERTY FOLDER "modules/nutrient")
ENDIF ()
IF (WIN32)
    TARGET_LINK_LIBRARIES(MINRL module_setting bmps data util)
ELSE ()
    TARGET_LINK_LIBRARIES(MINRL libmodule_setting.a bmps.a libdata.a libutil.a)
ENDIF ()