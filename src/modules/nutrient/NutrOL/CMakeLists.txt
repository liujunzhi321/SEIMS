LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} NutrientOLRoute.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(NUTROL SHARED ${SRC_LIST})
IF(WIN32)
	TARGET_LINK_LIBRARIES(NUTROL util module_setting)
	SET_TARGET_PROPERTIES(NUTROL PROPERTIES COMPILE_FLAGS "/openmp")
	SET_PROPERTY(TARGET NUTROL PROPERTY FOLDER "modules/nutrient")
ELSE()
	TARGET_LINK_LIBRARIES(NUTROL libutil libmodule_setting)
ENDIF(WIN32)