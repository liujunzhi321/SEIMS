LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} NutrientinGroundwater.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(NutGW SHARED ${SRC_LIST})
IF(WIN32)
	TARGET_LINK_LIBRARIES(NutGW util module_setting)
	SET_TARGET_PROPERTIES(NutGW PROPERTIES COMPILE_FLAGS "/openmp")
	SET_PROPERTY(TARGET NutGW PROPERTY FOLDER "modules/nutrient")
ELSE()
	TARGET_LINK_LIBRARIES(NutGW libutil libmodule_setting)
ENDIF(WIN32)