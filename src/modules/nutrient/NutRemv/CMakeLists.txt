LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} NutrientRemviaSr.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(NutRemv SHARED ${SRC_LIST})
IF(WIN32)
	TARGET_LINK_LIBRARIES(NutRemv util module_setting)
	SET_TARGET_PROPERTIES(NutRemv PROPERTIES COMPILE_FLAGS "/openmp")
	SET_PROPERTY(TARGET NutRemv PROPERTY FOLDER "modules/nutrient")
ELSE()
	TARGET_LINK_LIBRARIES(NutRemv libutil libmodule_setting)
ENDIF(WIN32)