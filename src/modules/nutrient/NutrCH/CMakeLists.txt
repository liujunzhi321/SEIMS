LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} NutrientCHRoute.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(NUTRCH SHARED ${SRC_LIST})
IF(WIN32)
	TARGET_LINK_LIBRARIES(NUTRCH util module_setting)
	SET_TARGET_PROPERTIES(NUTRCH PROPERTIES COMPILE_FLAGS "/openmp")
	SET_PROPERTY(TARGET NUTRCH PROPERTY FOLDER "modules/nutrient")
ELSE()
	TARGET_LINK_LIBRARIES(NUTRCH libutil libmodule_setting)
ENDIF(WIN32)