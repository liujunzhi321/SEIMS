LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} NutrientRemviaSr.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(NUTRMV SHARED ${SRC_LIST})
IF(WIN32)
	TARGET_LINK_LIBRARIES(NUTRMV util module_setting)
	SET_TARGET_PROPERTIES(NUTRMV PROPERTIES COMPILE_FLAGS "/openmp")
	SET_PROPERTY(TARGET NUTRMV PROPERTY FOLDER "modules/nutrient")
ELSE()
	TARGET_LINK_LIBRARIES(NUTRMV libutil libmodule_setting)
ENDIF(WIN32)