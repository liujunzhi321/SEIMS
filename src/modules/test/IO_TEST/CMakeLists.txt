LINK_DIRECTORIES(${PROJECT_BINARY_DIR})
SET(SRC_LIST ${MODULE_SRC} IO_test.cpp api.cpp)
SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR})
ADD_LIBRARY(IO_TEST SHARED ${SRC_LIST})
IF(WIN32)
	TARGET_LINK_LIBRARIES(IO_TEST util data module_setting)
	SET_TARGET_PROPERTIES(IO_TEST PROPERTIES COMPILE_FLAGS "/openmp")
	SET_PROPERTY(TARGET IO_TEST PROPERTY FOLDER "modules/test")
ELSE()
	TARGET_LINK_LIBRARIES(IO_TEST libutil libdata libmodule_setting)
ENDIF(WIN32)
